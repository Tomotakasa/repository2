# Firebase configuration
# 1. https://console.firebase.google.com でプロジェクトを作成
# 2. 「プロジェクトの設定」→「マイアプリ」→「ウェブアプリ」から取得
# 3. このファイルを .env にコピーして値を設定

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# NOTE: VITE_ prefix means these values are exposed to the browser.
# Firebase API keys are safe to expose — access is controlled by
# Firebase Security Rules (firestore.rules / storage.rules).
# Never put secret server keys (e.g., service account keys) in .env.
